<html lang="ko">

  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/index.css" />
  </head>

  <body>
    <h1><%= title %></h1>
    <p><%= name %></p>
    <%- elem %>
    <p>
      &lt%= 는 innerText이고<br />
      &lt%- 는 innerHTML이다
    </p>
    <% let a = 1; %>
    <p><%= a %></p>
    <% if(a < 10) { %>
    <div>a는 10보다 작아요</div>
    <% } %>
    <% if(a > 10) {%>
    <div>a는 10보다 커요</div>
    <% } %>
    <ul id="list">
      <li class="header">
        <ul class="row">
          <li class="num box-center">번호</li>
          <li class="title box-center">제목</li>
          <li class="writer box-center">글쓴이</li>
          <li class="createdAt box-center">등록일</li>
        </ul>

        <!-- 조회수 추천수 댓글수 -->
      </li>
      <li class="item notice">
        <ul class="row">
          <li class="num box-center">1</li>
          <li class="title">오늘의 점심은?</li>
          <li class="writer box-center">이정배</li>
          <li class="createdAt box-center">24-03-28</li>
        </ul>
      </li>
      <li class="item">
        <ul class="row">
          <li class="num">1</li>
          <li class="title">오늘의 점심은?</li>
          <li class="writer">이정배</li>
          <li class="createdAt">24-03-28</li>
        </ul>
      </li>
      <li class="item">
        <ul class="row">
          <li class="num">0</li>
          <li class="title"><%= title %></li>
          <li class="writer"><%= name %></li>
          <li class="createdAt">2024-04-03</li>
        </ul>
      </li>
      <% boards.forEach((item) =>{ %>
      <%- include('board/item', item); %>
      <% }) %>
      <!-- ejs의 함수, include는 해당 경로에 객체를 보낸다. 첫번째 매개변수는 경로, 두번째는 보내줄 객체를 입력해준다. -->
    </ul>
  </body>

</html>